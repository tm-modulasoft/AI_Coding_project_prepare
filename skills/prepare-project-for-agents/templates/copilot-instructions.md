Follow the root `AGENTS.md` as the source of truth for this repository.

When working in a specific area, also read the matching helper under `docs/agents/` (stack, conventions, best-practices, ui-ux, theming, testing, security).

Do not fork these rules into this file. Change `AGENTS.md` instead.
